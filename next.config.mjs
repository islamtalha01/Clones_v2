// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
  images: {
    domains: ["files2.heygen.ai"], // Add the external domain here
  },
};

export default nextConfig;
